const API_BASE_URL = "/api/subscription";

export const SUBSCRIPTION_API = {
  CREATE_SUBSCRIPTION:  `${API_BASE_URL}`,
  GET_SUBSCRIPTIONS: `${API_BASE_URL}/search-for-subscriber`,
  GET_SUBSCRIBERS: `${API_BASE_URL}/search-for-instructor`,
};
